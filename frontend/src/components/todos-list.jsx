import { Typography, Grid, Paper } from "@mui/material";

function TodoList() {
  return (
    <Grid container justifyContent="center">
      <Grid xs={12} item>
        <Typography variant="h5">Todolist page under construction </Typography>
      </Grid>
    </Grid>
  );
}

export default TodoList;
